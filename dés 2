<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Lanceur de Dé 3D</title>
  <style>
    *{box-sizing:border-box}
    body{
      display:flex;flex-direction:column;align-items:center;justify-content:center;
      min-height:100vh;margin:0;background:#f5f5f5;font-family:system-ui,Arial,sans-serif
    }
    .scene{width:140px;height:140px;perspective:700px;margin-bottom:20px}
    .dice{
      width:100%;height:100%;position:relative;transform-style:preserve-3d;
      transition:transform 1s cubic-bezier(.2,.8,.2,1)
    }
    .face{
      position:absolute;width:140px;height:140px;background:#fff;border:2px solid #222;
      border-radius:16px;display:flex;align-items:center;justify-content:center;
      font-size:44px;font-weight:700;user-select:none
    }
    .front  {transform:rotateY(0deg) translateZ(70px)}
    .back   {transform:rotateY(180deg) translateZ(70px)}
    .right  {transform:rotateY(90deg) translateZ(70px)}
    .left   {transform:rotateY(-90deg) translateZ(70px)}
    .top    {transform:rotateX(90deg) translateZ(70px)}
    .bottom {transform:rotateX(-90deg) translateZ(70px)}

    button{
      padding:12px 20px;font-size:16px;border:none;border-radius:10px;cursor:pointer;
      background:#222;color:#fff;box-shadow:0 6px 16px rgba(0,0,0,.15)
    }
    #result{margin-top:14px;font-size:18px;font-weight:700}
  </style>
</head>
<body>
  <div class="scene">
    <div class="dice" id="dice">
      <div class="face front">1</div>
      <div class="face back">6</div>
      <div class="face right">4</div>
      <div class="face left">3</div>
      <div class="face top">5</div>
      <div class="face bottom">2</div>
    </div>
  </div>

  <button id="roll">🎲 Lancer le dé</button>
  <div id="result" aria-live="polite"></div>

  <script>
    const dice = document.getElementById("dice");
    const resultDiv = document.getElementById("result");
    const btn = document.getElementById("roll");

    function rollDice(){
      const result = Math.floor(Math.random() * 6) + 1;

      let x=0,y=0;
      switch(result){
        case 1: x=0;   y=0;   break;
        case 2: x=-90; y=0;   break;
        case 3: x=0;   y=-90; break;
        case 4: x=0;   y=90;  break;
        case 5: x=90;  y=0;   break;
        case 6: x=0;   y=180; break;
      }
      // Ajoute des tours complets aléatoires pour un effet “vrai lancer”
      x += 360 * (Math.floor(Math.random()*3)+1);
      y += 360 * (Math.floor(Math.random()*3)+1);

      dice.style.transform = `rotateX(${x}deg) rotateY(${y}deg)`;
      resultDiv.textContent = "Résultat : " + result;
    }
    btn.addEventListener("click", rollDice);
  </script>
</body>
</html>
